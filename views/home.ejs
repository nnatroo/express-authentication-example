<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/home.css">
</head>
<body>
<div class="home-container">
    <h1>Welcome to our website</h1>

    <% if (user) { %>
        <p class="user-message">You are logged in as <strong><%= user.email %></strong>. Visit your <a href="/dashboard"
                                                                                                       class="dashboard-link">dashboard</a>.
        </p>
    <% } else { %>
        <p class="guest-message">
            Please <a href="/login" class="auth-link">login</a> or
            <a href="/register" class="auth-link">register</a> to continue.
        </p>
    <% } %>
</div>
</body>
</html>
